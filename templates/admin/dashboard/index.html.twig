{% extends 'admin_layout.html.twig' %}

{% block title %}{{ parent() }} - {{ 'Dashboard'|trans }}{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ 'Dashboard'|trans }}
        </div>
        <div class="card-body">
            <table class="table">
                <tbody>
                    <tr>
                        <td>{{ 'Quotes'|trans }}</td>
                        <td>{{ quotes_count }}</td>
                    </tr>
                    <tr>
                        <td>{{ 'Users'|trans }}</td>
                        <td>{{ users_count }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
