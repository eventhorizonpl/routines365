{% extends 'admin_layout.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin_workspace') }}">
            {{ 'Workspace'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item">
        <a href="{{ path('admin_cron_job_index') }}">
            {{ 'Cron jobs'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item active">
        {{ 'Edit a cron job'|trans }}
    </li>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ 'Edit a cron job'|trans }}

            {% if is_granted('ROLE_SUPER_ADMIN') %}
                {{ include('admin/cron_job/_delete_form.html.twig') }}
            {% endif %}
        </div>
        <div class="card-body">
            {{ include('admin/cron_job/_form.html.twig', {'button_label': 'Update'|trans}) }}
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} - {{ 'Edit a cron job'|trans }}
{% endblock %}
