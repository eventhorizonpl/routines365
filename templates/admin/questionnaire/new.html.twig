{% extends 'admin_layout.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin_workspace') }}">
            {{ 'Workspace'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item">
        <a href="{{ path('admin_questionnaire_index') }}">
            {{ 'Questionnaires'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item active">
        {{ 'Create questionnaire'|trans }}
    </li>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ 'Create questionnaire'|trans }}
            <a class="btn btn-secondary btn-sm float-right" href="{{ path('admin_questionnaire_index') }}">
                <i aria-hidden="true" class="bi bi-x-square"></i>
            </a>
        </div>
        <div class="card-body">
            {{ include('admin/questionnaire/_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} - {{ 'Create questionnaire'|trans }}
{% endblock %}
