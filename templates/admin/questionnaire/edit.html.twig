{% extends 'admin_layout.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin_workspace') }}">
            {{ 'Workspace'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item">
        <a href="{{ path('admin_questionnaire_index') }}">
            {{ 'Questionnaires'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item active">
        {{ 'Edit questionnaire'|trans }}
    </li>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ 'Edit questionnaire'|trans }}
            <a class="btn btn-info btn-sm float-right" href="{{ path('admin_questionnaire_index') }}">
                <i aria-hidden="true" class="bi bi-arrow-left-square"></i>
                {{ 'Back'|trans }}
            </a>
        </div>
        <div class="card-body">
            {{ include('admin/questionnaire/_form.html.twig', {'button_label': 'Update'|trans}) }}

            {% if is_granted('ROLE_SUPER_ADMIN') %}
                <br>
                {{ include('admin/questionnaire/_delete_form.html.twig') }}
            {% endif %}
        </div>
    </div>
{% endblock %}

{% block title %}{{ parent() }} - {{ 'Edit questionnaire'|trans }}{% endblock %}
