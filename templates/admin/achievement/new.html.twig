{% extends 'admin_layout.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin_workspace') }}">
            {{ 'Workspace'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item">
        <a href="{{ path('admin_achievement_index') }}">
            {{ 'Achievements'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item active">
        {{ 'Create achievement'|trans }}
    </li>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ 'Create achievement'|trans }}
            <a class="btn btn-info btn-sm float-right" href="{{ path('admin_achievement_index') }}">
                <i class="fas fa-arrow-left"></i>
                {{ 'Back'|trans }}
            </a>
        </div>
        <div class="card-body">
            {{ include('admin/achievement/_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block title %}{{ parent() }} - {{ 'Create achievement'|trans }}{% endblock %}
