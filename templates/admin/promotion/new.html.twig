{% extends 'admin_layout.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin_workspace') }}">
            {{ 'Workspace'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item">
        <a href="{{ path('admin_promotion_index') }}">
            {{ 'Promotions'|trans }}
        </a>
    </li>
    <li class="breadcrumb-item active">
        {{ 'Create a promotion'|trans }}
    </li>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ 'Create a promotion'|trans }}
            <a class="btn btn-secondary btn-sm float-right" href="{{ path('admin_promotion_index') }}">
                <i aria-hidden="true" class="bi bi-x-square"></i>
            </a>
        </div>
        <div class="card-body">
            {{ include('admin/promotion/_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} - {{ 'Create a promotion'|trans }}
{% endblock %}
