{{ include('email/_start.txt.twig') }}

We want to congratulate you on completing your routine.

{% if reminder %}
    Your next routine {{ reminder.routine.name }} starts at {{ reminder.routineStartDate ? reminder.routineStartDate|date('Y-m-d H:i:s')|format_datetime : '' }}.
{% endif %}

{{ quote }}

Thanks,
Routines365 Team
