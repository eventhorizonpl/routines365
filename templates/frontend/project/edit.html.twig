{% extends 'frontend_layout.html.twig' %}

{% block content %}
    <div class="row mb-5">
        <div class="col-md-10 offset-md-1">
            <div class="card bg-dark text-white">
                <div class="card-header">
                    {{ 'Edit a project'|trans }}
                    {{ include('frontend/project/_delete_form.html.twig') }}
                </div>
                <div class="card-body">
                    {{ include('frontend/project/_form.html.twig', {'button_label': 'Update'|trans}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} - {{ 'Edit a project'|trans }}
{% endblock %}
