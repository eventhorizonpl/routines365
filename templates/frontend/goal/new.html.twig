{% extends 'frontend_layout.html.twig' %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ 'Create Goal'|trans }}
        </div>
        <div class="card-body">
            {{ include('frontend/goal/_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block title %}{{ parent() }} - {{ 'Create Goal'|trans }} {% endblock %}
