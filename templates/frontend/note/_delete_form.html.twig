<form class="float-right" method="post" action="{{ path('frontend_note_delete', {'uuid': note.uuid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note.uuid) }}">
    <button aria-label="{{ 'Delete'|trans }}" class="btn btn-sm btn-danger" title="{{ 'Delete'|trans }}">
        <i aria-hidden="true" class="bi bi-trash"></i>
    </button>
    <a aria-label="{{ 'Information'|trans }}" class="btn btn-info btn-sm ml-2" href="{{ path('frontend_static_page_how_to_notes') }}" title="{{ 'Information'|trans }}">
        <i aria-hidden="true" class="bi bi-question-square"></i>
    </a>
    <a aria-label="{{ 'Close'|trans }}" class="btn btn-secondary btn-sm ml-2" href="{{ path('frontend_note_show', {'uuid': note.uuid}) }}" title="{{ 'Close'|trans }}">
        <i aria-hidden="true" class="bi bi-x-square"></i>
    </a>
</form>
