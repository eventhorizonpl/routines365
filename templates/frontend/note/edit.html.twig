{% extends 'frontend_layout.html.twig' %}

{% block content %}
    <div class="row mb-5">
        <div class="col-md-10 offset-md-1">
            <div class="card bg-dark text-white">
                <div class="card-header">
                    {{ 'Edit note'|trans }}
                    <a class="btn btn-light btn-sm float-right" href="{{ path('frontend_note_show', {'uuid': note.uuid}) }}">
                        <i class="fas fa-times"></i>
                        {{ 'Close'|trans }}
                    </a>
                </div>
                <div class="card-body">
                    {{ include('frontend/note/_form.html.twig', {'button_label': 'Update'|trans}) }}
                    <br>
                    {{ include('frontend/note/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}{{ parent() }} - {{ 'Edit note'|trans }}{% endblock %}
