{{ form_start(form) }}
    {{ form_errors(form) }}

    <div class="row">
        <div class="col-md-12">
            {{ form_row(form.title, {'attr': {'placeholder': 'Title of your note.'|trans}}) }}
        </div>
    </div>
    {% if not note.routine %}
        <div class="row">
            <div class="col-md-12">
                {{ form_row(form.routine, {'attr': {'class': 'custom-select'}}) }}
            </div>
        </div>
    {% endif %}
    <div class="row">
        <div class="col-md-12">
            {{ form_row(form.content, {'attr': {'placeholder': 'Content of your note.'|trans, 'rows': 7}}) }}
        </div>
    </div>
    <hr>
    <div class="row">
        <div class="col-md-12 text-center">
            <button class="btn btn-primary" type="submit">
                <i class="fas fa-save"></i>
                {{ button_label|default('Save'|trans) }}
            </button>
        </div>
    </div>
{{ form_end(form) }}
