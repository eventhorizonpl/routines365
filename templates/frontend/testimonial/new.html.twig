{% extends 'frontend_layout.html.twig' %}

{% block content %}
    <div class="row mb-5">
        <div class="col-md-10 offset-md-1">
            <div class="card">
                <div class="card-header">
                    {{ 'Testimonial'|trans }}
                </div>
                <div class="card-body">
                    {{ 'Please share your opinion about our system.'|trans }}
                    <hr>
                    {{ include('frontend/testimonial/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} - {{ 'Testimonial'|trans }}
{% endblock %}
