{% extends 'frontend_layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="card col-md-10 offset-md-1">
            <div class="card-body">
                <h1 class="card-title h3">
                    {{ 'Landing pages'|trans }}
                </h1>
                <hr>
                <div class="lead">
                    <a href="{{ path('frontend_landing_page_learn_faster') }}">Learn faster</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} - {{ 'Landing pages'|trans }}
{% endblock %}
