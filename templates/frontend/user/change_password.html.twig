{% extends 'frontend_layout.html.twig' %}

{% block content %}
    <div class="row mb-5">
        <div class="col-md-10 offset-md-1">
            {% include('frontend_settings_nav.html.twig') %}
            <div class="card">
                <div class="card-header">
                    {{ 'Change password'|trans }}
                </div>
                <div class="card-body">
                    {{ include('frontend/user/_form.html.twig', {'button_label': 'Change password'|trans}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}{{ parent() }} - {{ 'Change password'|trans }} {% endblock %}
