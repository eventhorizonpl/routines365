{% extends 'frontend_layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="card col-md-10 offset-md-1">
            <div class="card-body">
                <h5 class="card-title">
                    <a href="{{ path('frontend_user_kyt_start') }}">{{ 'Know your tools'|trans }}</a> - {{ 'Goals'|trans }}
                </h5>
                <hr>
                {{ include('frontend/static_page/how_to/_goals_start.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} - {{ 'Know your tools'|trans }} - {{ 'Goals'|trans }}
{% endblock %}
